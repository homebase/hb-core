* checkout https://github.com/homebase/php-tools somewhere (suggestion: ~/src/)
* run `./install all` there
* create symlink `ln -s $PHP_TOOLS_PATH/php-tools/bin tools`

* to make tests runnable place create symlink for `stest` in your `~/bin` or `/usr/local/bin`
